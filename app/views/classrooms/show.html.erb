Classroom Info
<%= render partial: "classroom", locals: {local_assigns: @classroom } %>
<br>
<% @classroom.students.each do |student| %>
<%= render partial: "students/student", locals: {local_assigns: student} %>
<% end %>
